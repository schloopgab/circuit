import React from "react";
import ReactDOM from "react-dom/client";
import CircuitApp from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <CircuitApp />
  </React.StrictMode>
);
